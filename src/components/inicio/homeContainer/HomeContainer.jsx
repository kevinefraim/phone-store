import Cards from "../cards/Cards";
import Slider from "../slider/Slider";

const HomeContainer = () => {
  return (
    <main>
      <Slider />
      <Cards />
    </main>
  );
};

export default HomeContainer;
